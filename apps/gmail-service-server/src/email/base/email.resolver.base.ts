/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Email } from "./Email";
import { EmailCountArgs } from "./EmailCountArgs";
import { EmailFindManyArgs } from "./EmailFindManyArgs";
import { EmailFindUniqueArgs } from "./EmailFindUniqueArgs";
import { CreateEmailArgs } from "./CreateEmailArgs";
import { UpdateEmailArgs } from "./UpdateEmailArgs";
import { DeleteEmailArgs } from "./DeleteEmailArgs";
import { LabelFindManyArgs } from "../../label/base/LabelFindManyArgs";
import { Label } from "../../label/base/Label";
import { Thread } from "../../thread/base/Thread";
import { EmailService } from "../email.service";
@graphql.Resolver(() => Email)
export class EmailResolverBase {
  constructor(protected readonly service: EmailService) {}

  async _emailsMeta(
    @graphql.Args() args: EmailCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Email])
  async emails(@graphql.Args() args: EmailFindManyArgs): Promise<Email[]> {
    return this.service.emails(args);
  }

  @graphql.Query(() => Email, { nullable: true })
  async email(
    @graphql.Args() args: EmailFindUniqueArgs
  ): Promise<Email | null> {
    const result = await this.service.email(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Email)
  async createEmail(@graphql.Args() args: CreateEmailArgs): Promise<Email> {
    return await this.service.createEmail({
      ...args,
      data: {
        ...args.data,

        thread: args.data.thread
          ? {
              connect: args.data.thread,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Email)
  async updateEmail(
    @graphql.Args() args: UpdateEmailArgs
  ): Promise<Email | null> {
    try {
      return await this.service.updateEmail({
        ...args,
        data: {
          ...args.data,

          thread: args.data.thread
            ? {
                connect: args.data.thread,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Email)
  async deleteEmail(
    @graphql.Args() args: DeleteEmailArgs
  ): Promise<Email | null> {
    try {
      return await this.service.deleteEmail(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [Label], { name: "labels" })
  async findLabels(
    @graphql.Parent() parent: Email,
    @graphql.Args() args: LabelFindManyArgs
  ): Promise<Label[]> {
    const results = await this.service.findLabels(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => Thread, {
    nullable: true,
    name: "thread",
  })
  async getThread(@graphql.Parent() parent: Email): Promise<Thread | null> {
    const result = await this.service.getThread(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
